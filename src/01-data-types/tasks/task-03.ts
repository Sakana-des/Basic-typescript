/**
 * SMK Telkom Malang is developing a new Student Information System. 
 * Before students can use the system, the administrator must record each student's basic information. 
 * Every student has a unique student ID, a full name, an age, and a status indicating whether they are currently an active student.
 * 
 * Task:
 * 1. Define a proper type for the student information.
 * 2. Implement a type that you defined on 3 students data.
 * 
 * display the student data using console.log.
 */
const Fullname : Array<string> = ["Lionel pesi", "Cristiano pesi", "El mbappe pesi"];
const StudentID : Array<string> = ["TS1-1", "TS2-1", "TS3-1"];
const Age : Array<number> = [17, 18, 16];
const status_active : Array<boolean> = [true, true, false];

console.log("Administration Data Student list");
console.log({StudentID });
console.log({Fullname});
console.log({Age});
console.log({status_active});
           